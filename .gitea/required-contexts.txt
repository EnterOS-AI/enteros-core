# SSOT: the ENFORCED "required to merge" status contexts on molecule-core
# main. This file is now AUTHORITATIVE AND ENFORCED (internal#3181):
#
#   1. lint-no-coe-on-required reads it as the required-context allowlist
#      (CI cannot always read branch_protections; when a repo-admin token
#      is present the lint also live-cross-checks against BP and fails when
#      live BP has a context NOT in this list).
#   2. THE MERGE QUEUE (gitea-merge-queue.py, the bot that actually performs
#      merges via the operator conductor tick) now ENFORCES every entry
#      ABOVE the `# pending-` marker below: each must be `success` on the PR
#      head before the PR can merge, fail-closed and event-suffix-insensitive
#      — REGARDLESS of whether the entry is in branch-protection
#      status_check_contexts. This closes the gap where a context listed
#      here but absent from BP was treated as a non-required ADVISORY red
#      and force_merge=true bypassed it (PR#3181 merged with
#      `E2E Staging SaaS (full lifecycle) / E2E Staging Concierge Creates
#      Workspace` RED for exactly this reason).
#
# Form: "<workflow name> / <job name>" (event suffix stripped — the gate
# strips `(pull_request)` / `(push)` / `(pull_request_target)` on both
# sides before matching). Keep this list curated: anything ABOVE the
# pending marker WILL block every merge if it is red.
#
# ── SEQUENCING (READ BEFORE EDITING) ────────────────────────────────────
# Entries AT OR BELOW the first `# pending-#NNNN (not yet enforced)` marker
# line are DOCUMENTED-required but NOT enforced by the merge queue. This is
# the escape hatch that lets a known-red, separately-tracked check live in
# the SSOT without freezing the whole queue. To PROMOTE a parked context to
# enforced: verify it is green on main, then MOVE its line above the marker
# (and ideally collapse the marker once its section is empty). To PARK a
# context that has started failing for a tracked reason: move it below the
# marker and reference the tracking issue, rather than deleting it.
# ────────────────────────────────────────────────────────────────────────

# ── ENFORCED (merge-blocking) ──────────────────────────────────────────
CI / all-required
E2E API Smoke Test / E2E API Smoke Test
Handlers Postgres Integration / Handlers Postgres Integration
E2E Peer Visibility (literal MCP list_peers) / E2E Peer Visibility
Secret scan / Scan diff for credential-shaped strings
template-delivery-e2e / Template-asset delivery (fresh seo-agent — config+prompts via asset channel, seo-all via plugin reconcile)
# SOP/review ceremony gates REMOVED from the required set 2026-07-11 (CTO
# directive: bot-operated review/SOP acks were meaningless ceremony). Their
# producer workflows (qa-review, security-review, sop-checklist, gate-check-v3)
# are disabled_manually, so keeping them here made them phantom gates —
# required-on-paper but silently unenforced (lint-phantom-gate Guard F). Real
# protection stays: CI ['*'] + secret-scan. To restore the ceremony, re-enable
# the producer workflows AND re-add their contexts below AND restore the
# pull_request_target triggers that task #97 removed (all three, or you get
# either a phantom gate or a wedge).
#
# CORRECTION 2026-07-13: this list used to name `reserved-path-review` among the
# disabled producers. That was WRONG — reserved-path-review is ACTIVE and has
# been gating all along. Believing the stale claim cost real time while
# debugging the wedge in task #89; the Gitea Actions API is the SSOT for
# workflow state, not this comment.
Concierge Creates Workspace Hermetic / Concierge Creates Workspace Hermetic

# RE-ENABLED on MOLECULES-SERVER 2026-07-03 — enforcement SEQUENCED ─────────
# `E2E Staging SaaS (full lifecycle) / E2E Staging Concierge Creates Workspace`
# is NO LONGER workflow_dispatch-only: its producer (e2e-staging-saas.yml) was
# re-enabled on push + pull_request and the job now provisions on the
# MOLECULES-SERVER (local-docker, provider='local') backend — no AWS creds, no
# EC2. It runs as a DEPLOY GATE on push-to-main (continue-on-error:false; a
# cold-open /workspaces failure REDs the run), and its full-journey script now
# also carries the COLD-OPEN browser-slug assertion (STEP 2.5) that catches the
# in-browser /workspaces 404 the admin-bearer probe misses.
#
# CORRECTION 2026-07-14 — this block used to claim: "It is kept BELOW the ENFORCED
# marker (documented-required, not merge-blocking)". THAT WAS FALSE. The line is
# physically ABOVE the first `# pending-#NNNN` marker, and gitea-merge-queue.py
# (_PENDING_MARKER_RE) enforces everything above that marker regardless of the
# prose around it. So this context has been merge-blocking all along, while the
# file said it wasn't. The file lied about its own enforcement.
#
# Worse, its promotion condition #2 was: "the PR/fork path posts `success` WITHOUT
# staging creds (E2E_REQUIRE_LIVE=0 PR-mode self-check)". That is a VACUOUS PASS
# written down as a requirement. Enforcement is evaluated on the PR HEAD, so an
# enforced context whose PR arm always succeeds — by running `bash -n` on a shell
# script and exiting 0 — enforces exactly nothing. It was a merge gate that could
# not fail a merge.
#
# FIXED (2026-07-14): the producer now runs the REAL live concierge E2E on
# same-repo pull requests (E2E_REQUIRE_LIVE=1, Infisical fetch enabled for
# non-forks, fail-closed on an absent token). A FORK PR — which genuinely cannot
# hold the staging credential — gets a LOUD ::warning:: skip that says the gate did
# not run and a maintainer must re-run it from a same-repo branch, rather than a
# silent green.
#
# Safe to enforce for real: this job is green on every recent main push (5/5
# checked). It was never broken — it just never ran where enforcement happens.
E2E Staging SaaS (full lifecycle) / E2E Staging Concierge Creates Workspace

# pending-#2409 (not yet enforced) ──────────────────────────────────────
# The local-docker real-tenant provision lane (local-provision-e2e.yml,
# job "Local Provision Lifecycle E2E (stub)") provisions a REAL workspace
# via the local Docker provisioner and asserts the full lifecycle
# (provision → online → restart-survive → proxy-reach → GET /workspaces
# usability). It runs per-PR + push:main/staging and gates within the
# workflow (continue-on-error: false). It is DOCUMENTED-required here and
# is the intended cheap-per-PR replacement for the AWS staging concierge
# gate; PROMOTE above once the operator confirms docker-host runners
# reliably provision sibling containers from the host platform binary
# (#2409 substrate confirmation).
Local Provision Lifecycle E2E / Local Provision Lifecycle E2E (stub)

# pending-#3159 (not yet enforced) ──────────────────────────────────────
# The E2E Staging SaaS context below is DOCUMENTED-required but is
# currently RED, blocked by #3159 (a separate runtime PR is fixing it in
# parallel). It is PARKED here so this enforcement change can ship
# WITHOUT freezing every merge. SEQUENCING: once #3159 is green on main
# and this context is reliably `success`, PROMOTE it by moving the line
# above this marker (and remove this block). Do NOT promote it while
# #3159 is open — doing so will freeze the merge queue.
#
# #48 (RCA molecule-controlplane #878→#885): real platform-managed boot is
# the intended merge gate.
E2E Staging SaaS (full lifecycle) / E2E Staging Platform Boot
E2E Ephemeral CP Happy Path / E2E Ephemeral CP Happy Path
