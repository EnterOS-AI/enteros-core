# SSOT: the ENFORCED "required to merge" status contexts on molecule-core
# main. This file is now AUTHORITATIVE AND ENFORCED (internal#3181):
#
#   1. lint-no-coe-on-required reads it as the required-context allowlist
#      (CI cannot always read branch_protections; when a repo-admin token
#      is present the lint also live-cross-checks against BP and fails when
#      live BP has a context NOT in this list).
#   2. THE MERGE QUEUE (gitea-merge-queue.py, the bot that actually performs
#      merges via the operator conductor tick) now ENFORCES every entry
#      ABOVE the `# pending-` marker below: each must be `success` on the PR
#      head before the PR can merge, fail-closed and event-suffix-insensitive
#      — REGARDLESS of whether the entry is in branch-protection
#      status_check_contexts. This closes the gap where a context listed
#      here but absent from BP was treated as a non-required ADVISORY red
#      and force_merge=true bypassed it (PR#3181 merged with
#      `E2E Staging SaaS (full lifecycle) / E2E Staging Concierge Creates
#      Workspace` RED for exactly this reason).
#
# Form: "<workflow name> / <job name>" (event suffix stripped — the gate
# strips `(pull_request)` / `(push)` / `(pull_request_target)` on both
# sides before matching). Keep this list curated: anything ABOVE the
# pending marker WILL block every merge if it is red.
#
# ── SEQUENCING (READ BEFORE EDITING) ────────────────────────────────────
# Entries AT OR BELOW the first `# pending-#NNNN (not yet enforced)` marker
# line are DOCUMENTED-required but NOT enforced by the merge queue. This is
# the escape hatch that lets a known-red, separately-tracked check live in
# the SSOT without freezing the whole queue. To PROMOTE a parked context to
# enforced: verify it is green on main, then MOVE its line above the marker
# (and ideally collapse the marker once its section is empty). To PARK a
# context that has started failing for a tracked reason: move it below the
# marker and reference the tracking issue, rather than deleting it.
# ────────────────────────────────────────────────────────────────────────

# ── ENFORCED (merge-blocking) ──────────────────────────────────────────
CI / all-required
E2E API Smoke Test / E2E API Smoke Test
Handlers Postgres Integration / Handlers Postgres Integration
E2E Peer Visibility (literal MCP list_peers) / E2E Peer Visibility
Secret scan / Scan diff for credential-shaped strings
template-delivery-e2e / Template-asset delivery (fresh seo-agent — config+prompts via asset channel, seo-all via plugin reconcile)
# #3141 (CTO-authorized): the three review-team gates are merge-blocking.
# They post commit statuses on pull_request_target / pull_request_review.
# The merge queue already enforces qa-review/security-review/sop-checklist
# via GOVERNANCE_REQUIRED_CONTEXTS; listing them here makes the SSOT
# complete and keeps lint-no-coe-on-required honest. Event suffix stripped.
qa-review / approved
security-review / approved
sop-checklist / all-items-acked
reserved-path-review / reserved-path-review
Concierge Creates Workspace Hermetic / Concierge Creates Workspace Hermetic
E2E Staging SaaS (full lifecycle) / E2E Staging Concierge Creates Workspace

# pending-#3159 (not yet enforced) ──────────────────────────────────────
# The E2E Staging SaaS context below is DOCUMENTED-required but is
# currently RED, blocked by #3159 (a separate runtime PR is fixing it in
# parallel). It is PARKED here so this enforcement change can ship
# WITHOUT freezing every merge. SEQUENCING: once #3159 is green on main
# and this context is reliably `success`, PROMOTE it by moving the line
# above this marker (and remove this block). Do NOT promote it while
# #3159 is open — doing so will freeze the merge queue.
#
# #48 (RCA molecule-controlplane #878→#885): real platform-managed boot is
# the intended merge gate.
E2E Staging SaaS (full lifecycle) / E2E Staging Platform Boot
