name: runtime-default-flip-gate

# Guard B config/pin-flip coupling: a change to the platform DEFAULT RUNTIME
# (KMS MOLECULE_DEFAULT_RUNTIME) or an openclaw runtime-image pin MUST go through
# the fresh-org platform-agent CALLABLE gate before it takes effect — so an
# out-of-gate openclaw advance or a default-flip skew (task#225/#226/test123)
# can never reach users un-gated.
#
# It wraps scripts/deploy/gate-runtime-default-flip.sh, which: advances the
# proposed value, runs TestPlatformAgentMgmtMCP_Staging with
# E2E_ASSERT_MGMT_MCP_CALLABLE=1 (a REAL A2A provision_workspace turn on the
# proposed runtime), KEEPS the flip on GREEN and REVERTS it on RED.
#
# Repository policy keeps dispatch flat, so the proposed value is read from
# repo/org VARS (set the var, dispatch, then unset):
#   RUNTIME_FLIP_PROPOSED_RUNTIME  e.g. openclaw   (default-runtime flip)
#   RUNTIME_FLIP_KEY / RUNTIME_FLIP_VALUE          (arbitrary pin key/value)
#   RUNTIME_FLIP_APPLY=1                           (WRITE + gate; default: dry-run/plan-only)
#
# SAFETY: default is DRY-RUN (plan only, zero writes). --apply writes exactly one
# Infisical secret and self-reverts it on a red gate.

on:
  workflow_dispatch: {}
  # Also fire when the gate wrapper or the gate test itself changes, so a change
  # to the coupling is itself exercised (dry-run — no writes without the var).
  push:
    branches: [main]
    paths:
      - 'scripts/deploy/gate-runtime-default-flip.sh'
      - '.gitea/workflows/runtime-default-flip-gate.yml'
      - 'workspace-server/internal/staginge2e/platform_agent_mgmt_mcp_e2e_test.go'
      - 'workspace-server/internal/staginge2e/platform_agent_mgmt_mcp_gate.go'

concurrency:
  group: runtime-default-flip-gate
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  # This context (runtime-default-flip-gate / flip-gate) fires only on push to
  # main + workflow_dispatch — never on pull_request — so it is not a PR merge gate.
  # bp-exempt: push/dispatch coupling EXERCISER (dry-run default; zero writes without RUNTIME_FLIP_APPLY). The hard runtime-default-flip gate is enforced on the apply path (gate-runtime-default-flip.sh self-reverts on red) + staging-tenant-cd.yml e2e-smoke, not this status.
  flip-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      CP_BASE_URL: https://staging-api.moleculesai.app
      E2E_PROVIDER: molecules-server
      # Consumed only by the `if: always()` teardown safety net below (the purge
      # helper tests/e2e/lib/cp_purge_receipt.sh origin/backend guards). The Go
      # test itself reads E2E_PROVIDER, not this.
      E2E_INFRA_BACKEND: local-docker
      PROPOSED_RUNTIME: ${{ vars.RUNTIME_FLIP_PROPOSED_RUNTIME }}
      FLIP_KEY: ${{ vars.RUNTIME_FLIP_KEY }}
      FLIP_VALUE: ${{ vars.RUNTIME_FLIP_VALUE }}
      FLIP_APPLY: ${{ vars.RUNTIME_FLIP_APPLY }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: 'stable'
          cache: false
          cache-dependency-path: workspace-server/go.sum
      - name: Fetch staging CP admin token from Infisical KMS
        env:
          INFISICAL_CI_CLIENT_ID: ${{ secrets.INFISICAL_CI_CLIENT_ID }}
          INFISICAL_CI_CLIENT_SECRET: ${{ secrets.INFISICAL_CI_CLIENT_SECRET }}
          INFISICAL_PROJECT_ID: ${{ secrets.INFISICAL_CI_PROJECT_ID }}
        run: |
          set -uo pipefail
          BASE="https://key.moleculesai.app"
          TOK=$(curl -fsS -X POST "$BASE/api/v1/auth/universal-auth/login" \
            -H 'Content-Type: application/json' \
            -d "{\"clientId\":\"$INFISICAL_CI_CLIENT_ID\",\"clientSecret\":\"$INFISICAL_CI_CLIENT_SECRET\"}" \
            | python3 -c 'import sys,json; d=json.load(sys.stdin); v=d.get("accessToken"); sys.stdout.write(v if isinstance(v,str) and v else "")')
          if [ -z "$TOK" ]; then echo "::error::Infisical accessToken empty - failing closed"; exit 1; fi
          CP_ADMIN_API_TOKEN=$(curl -fsS "$BASE/api/v3/secrets/raw/CP_ADMIN_API_TOKEN?workspaceId=$INFISICAL_PROJECT_ID&environment=staging&secretPath=%2Fshared%2Fcontrolplane-admin" \
            -H "Authorization: Bearer $TOK" \
            | python3 -c 'import sys,json; d=json.load(sys.stdin); v=(d.get("secret") or {}).get("secretValue"); sys.stdout.write(v if isinstance(v,str) and v else "")')
          echo "::add-mask::$CP_ADMIN_API_TOKEN"
          if [ -z "$CP_ADMIN_API_TOKEN" ]; then
            echo "::error::Infisical returned empty CP_ADMIN_API_TOKEN at staging /shared/controlplane-admin — failing closed."
            exit 1
          fi
          echo "CP_ADMIN_API_TOKEN=$CP_ADMIN_API_TOKEN" >> "$GITHUB_ENV"
          echo "CP_ADMIN_API_TOKEN loaded from Infisical staging SSOT (len=${#CP_ADMIN_API_TOKEN})"
      - name: Gate the proposed runtime-default / pin flip on the CALLABLE e2e
        env:
          INFISICAL_CLIENT_ID: ${{ secrets.INFISICAL_CI_CLIENT_ID }}
          INFISICAL_CLIENT_SECRET: ${{ secrets.INFISICAL_CI_CLIENT_SECRET }}
          INFISICAL_PROJECT_ID: ${{ secrets.INFISICAL_CI_PROJECT_ID }}
        run: |
          set -euo pipefail
          if [ -z "${PROPOSED_RUNTIME:-}" ] && [ -z "${FLIP_KEY:-}" ]; then
            echo "::notice::no RUNTIME_FLIP_PROPOSED_RUNTIME / RUNTIME_FLIP_KEY var set — nothing to gate. Set the var + dispatch to gate a flip."
            exit 0
          fi
          ARGS=()
          [ -n "${PROPOSED_RUNTIME:-}" ] && ARGS+=(--runtime "$PROPOSED_RUNTIME")
          [ -n "${FLIP_KEY:-}" ]   && ARGS+=(--key "$FLIP_KEY")
          [ -n "${FLIP_VALUE:-}" ] && ARGS+=(--value "$FLIP_VALUE")
          if [ "${FLIP_APPLY:-0}" = "1" ]; then
            ARGS+=(--apply)
            echo "::warning::RUNTIME_FLIP_APPLY=1 — this WILL write the proposed value, gate it, and keep-or-revert."
          else
            ARGS+=(--dry-run)
            echo "::notice::dry-run (default) — planning only, zero writes. Set RUNTIME_FLIP_APPLY=1 to apply through the gate."
          fi
          bash scripts/deploy/gate-runtime-default-flip.sh "${ARGS[@]}"
      # Belt-and-braces teardown: the Go test installs a t.Cleanup admin-DELETE
      # of its own tenant, but Go SKIPS t.Cleanup when `go test -timeout` fires
      # or the runner is SIGKILLed — so a timed-out/cancelled Go test leaks its
      # org. This net purges any CP admin org whose slug carries THIS run's
      # GITHUB_RUN_ID for the exact tag(s) this job creates (e2eSlug formats
      # slugs as e2e-<tag>-<run_id>-<uniq>), via the SAME identity-bound purge
      # helper the shell jobs use (cp_purge_receipt.sh) — no new auth path.
      # Idempotent with t.Cleanup; a fast no-op on the happy path.
      - name: Teardown safety net (runs on timeout/cancel/failure)
        # push/dispatch-only workflow; the PR guard is a uniform no-op here.
        if: always() && github.event_name != 'pull_request'
        env:
          # Sourced from Infisical (staging /shared/controlplane-admin) via the
          # "Fetch staging CP admin token from Infisical KMS" step above.
          ADMIN_TOKEN: ${{ env.CP_ADMIN_API_TOKEN }}
          # Exact tag THIS job creates: the gate wrapper runs
          # TestPlatformAgentMgmtMCP_Staging → e2eSlug("mcp") (only when a flip
          # var is set; otherwise no org exists and the roster filter is empty).
          TEARDOWN_TAGS: "mcp"
        run: bash tests/e2e/lib/go_e2e_run_teardown.sh
