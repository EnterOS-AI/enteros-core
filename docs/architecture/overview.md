# Architecture overview

The maintained architecture overview is [System Architecture](./architecture.md).
For code-backed ownership, compatibility fields, validation rules, and links to
the executable sources, use the [Core technical
reference](./molecule-technical-doc.md).

This compatibility page intentionally does not maintain a second runtime list,
cloud topology, route count, or lifecycle description. Those duplicated claims
drifted from `manifest.json`, the router, and the active provisioning backends.
