# Harness-replay public Python dependencies.
#
# The published molecules-workspace-runtime wheel is installed separately by
# scripts/install-workspace-runtime.sh. Keep this file limited to public
# packages used directly by harness replays; do not duplicate the private
# runtime package or configure a mixed package index here.

httpx>=0.28.1

# channel-envelope-trust-boundary.sh imports the installed
# `molecule_runtime.*` modules, whose A2A client uses this HTTP surface. The
# explicit dependency keeps the replay environment clear and independent of
# repository-local runtime source.
